services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .dev.vars
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8788
    ports:
      - "8788:8788"
    restart: unless-stopped
